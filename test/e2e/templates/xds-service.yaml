apiVersion: v1
kind: Service
metadata:
  name: {{ .XdsServiceName }}
  namespace: {{ .TestNamespace }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: {{ .XdsServerPort }}
    protocol: TCP
    name: grpc

