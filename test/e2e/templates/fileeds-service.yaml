apiVersion: v1
kind: Service
metadata:
  name: {{ .FileEdsServiceName }}
  namespace: {{ .EnvoyGatewayNamespace }}
spec:
  selector:
    app: {{ .FileEdsDeploymentName }}
  ports:
  - port: {{ .FileEdsPort }}
    targetPort: {{ .FileEdsPort }}
    protocol: TCP
    name: grpc

