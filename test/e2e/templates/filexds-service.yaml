apiVersion: v1
kind: Service
metadata:
  name: {{ .FileXdsServiceName }}
  namespace: {{ .EnvoyGatewayNamespace }}
spec:
  selector:
    app: {{ .FileXdsDeploymentName }}
  ports:
  - port: {{ .FileXdsPort }}
    targetPort: {{ .FileXdsPort }}
    protocol: TCP
    name: grpc

