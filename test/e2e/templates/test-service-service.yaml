apiVersion: v1
kind: Service
metadata:
  name: {{ .TestServiceName }}
  namespace: {{ .TestNamespace }}
spec:
  selector:
    app: {{ .TestServiceName }}
  ports:
  - port: {{ .TestServicePort }}
    targetPort: {{ .TestServicePort }}
    protocol: TCP
    name: http

