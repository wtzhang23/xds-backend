apiVersion: v1
kind: Secret
metadata:
  name: {{ .TestServiceTLSSecretName }}
  namespace: {{ .TestNamespace }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .TestServiceTLSCertBase64 }}
  tls.key: {{ .TestServiceTLSKeyBase64 }}

