apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xds-backend.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "xds-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xds-backend.fullname" . }}:{{ .Values.service.httpPort }}']
  restartPolicy: Never
